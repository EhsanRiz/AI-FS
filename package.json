{
  "name": "ai-fs",
  "version": "1.0.0",
  "private": true,
  "description": "AI-FS — Field Supervisor Monitoring PWA. Dev tooling only; the app itself has no build step and ships as static files from the repo root.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "serve": "node tests/serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
